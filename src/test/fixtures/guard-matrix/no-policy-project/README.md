# Proyecto sin política

Fixture de la matriz multi-proyecto (Fase 4/028A-6): este directorio NO tiene
`sentinel.config.json` ni `quality.config.json`, por lo que el guard debe
dejarlo pasar (pass-through, sin bloqueo ni observación).
